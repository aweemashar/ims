{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"],
      "phases": {
        "install": {
          "cmds": [
            "pip install -r src/backend/requirements.txt"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "python src/backend/manage.py migrate --noinput && python src/backend/manage.py collectstatic --noinput && gunicorn --chdir src/backend InvenTree.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
